#include "commands.h"

#include <stdio.h>

void cmd_create(const char *name) {
    printf("cmd_create");
}
void cmd_pick(const char *name) {
    printf("cmd_pick");
}

void cmd_snap(const char *name) {
    printf("cmd_snap");
}

void cmd_drop(const char *name) {
    printf("cmd_drop");
}

void cmd_delete(const char *name) {
    printf("cmd_delete");
}

