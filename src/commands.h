
void cmd_create(const char *name); // Create frost-env
void cmd_pick(const char *name);   // Pick and init frost-env in saves snapshots
void cmd_snap(const char *name);   // Save snapshot
void cmd_drop(const char *name);   // Drop frost-env (onmount and delete simlinks)
void cmd_delete(const char *name); // Delete snapshot(s)
